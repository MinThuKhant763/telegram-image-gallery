{
  "name": "telegram-image-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "A Telegram-powered image gallery website using Supabase storage and database.",
  "scripts": {
    "dev": "node server.js",
    "check": "node --check server.js && find api -name '*.js' -print0 | xargs -0 -n1 node --check && node --check public/theme.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  }
}
