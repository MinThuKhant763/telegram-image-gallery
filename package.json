{
  "name": "telegram-image-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "A Telegram-powered image gallery website using Supabase storage and database.",
  "scripts": {
    "dev": "node server.js",
    "check": "node --check server.js && node --check api/telegram-webhook.js && node --check api/gallery.js && node --check api/notes.js && node --check api/admin-images.js && node --check api/admin-notes.js && node --check api/public-config.js && node --check public/theme.js"
  },
  "engines": {
    "node": ">=18"
  }
}
